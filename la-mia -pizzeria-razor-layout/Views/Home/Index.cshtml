@*@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>*@

@{
    Layout=null;
}

<html>
    
@*1. Dichiarare una variabile valorizzandola con il nostro nome.
Dichiarare un'altra variabile valorizzandola con il nostro cognome.
Stampare il nostro nome e cognome all'interno di un tag h2.*@

@{ var Nome = "Luca"; }

@{ var Cognome = "Vanossi"; }

<div><h2>@Nome, @Cognome</h2></div>

@*2. Creare una funzione che stampa la somma dei numeri che vanno da 1 a n (con n passato come parametro - n incluso)
Stampare dentro uno span il risultato.*@

@{
   int StampaSommaNumeri(int numeroMassimo)
   {
         int somma = 0;
         for(int i = 1; i <= numeroMassimo; i++)
   {
         somma += i;
   }
        return somma;
   }
   }
   @{
      int numeroMassimo = 13;
    }
      <p>La somma dei numeri da 1 a @numeroMassimo è: @StampaSommaNumeri(@numeroMassimo)</p>

@*3. Contare quante volte la lettera a è presente nalla frase "nel mezzo del cammin di nostra vita" 
e stampare questo valore in pagina*@

@{
                    int ContaLettere(char letteraDaContare, string fraseInCuiContareLaLettera)
                    {
                        int numeroPresenzaLettera = 0;
                        fraseInCuiContareLaLettera = fraseInCuiContareLaLettera.ToLower();
                        char[] caratteri = fraseInCuiContareLaLettera.ToCharArray();
                        for (int i = 0; i < fraseInCuiContareLaLettera.Length; i++)
                       {
                            if(letteraDaContare == caratteri[i])
                            {
                                numeroPresenzaLettera++;
                            }
                        }
                        return numeroPresenzaLettera;
                    }
                }
                @{
                    char lettera = 'a';
                    string frase = "nel mezzo del cammin di nostra vita";
                }
                <p>La lettera "<b>@lettera</b>" è presente nella frase "<b>@frase</b>" @ContaLettere(lettera, frase) volte.</p>

@*4. Creare una List<int> di 20 numeri interi (generati random da 1 a 90, 1 e 90 inclusi) 
    e mostrare in pagina solo i numeri compresi tra 20 e 70*@

     @{
                    List<int> listaDi20Interi = new List<int>();
                    Random random = new Random();
                    for (int i = 0; i < 20; i++)
                    {
                        listaDi20Interi.Add(random.Next(1, 91));
                    }
                }
                <p>I numeri compresi tra 20 e 70 (estremi inclusi) sono:</p>
                <ol>
                    @foreach(int numero in listaDi20Interi)
                    {
                        if(numero >= 20 & numero <= 70)
                        {
                            <li>@numero</li>
                        }
                    }

@*5. Stampare in pagina la data e l'ora attuale (usando la classe DateTime) con il formato anno-mese-giorno ore:minuti:secondi*@

@{
                    DateTime dataDiOggi = DateTime.Now;
                }
                <p>@dataDiOggi.ToString("yyyy-MM-dd hh:mm:ss")</p>

@*6. Partendo dalla data di oggi (usando la classe DateTime) stampare in pagina
a. la data di 7 giorni fa
b. che giorno della settimana è oggi (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
c. che giorno della settimana sarà nello stesso giorno e mese di oggi ma tra un anno 
(il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
*@

</html>



